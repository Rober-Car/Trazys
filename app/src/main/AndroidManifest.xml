<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Notificaciones (Android 13+). El ADMIN recibe avisos (p. ej. solicitudes
         de baja) por el canal de push exclusivo del administrador. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- TODO(PRODUCCION): allowBackup desactivado en desarrollo para que Google no restaure
         BDs antiguas al reinstalar. Antes de publicar: decidir si se reactiva y con qué
         reglas de exclusion (backup_rules.xml / data_extraction_rules.xml) -->
    <application
        android:allowBackup="false"
        android:name=".GestorProApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestorPro">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.GestorPro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servicio FCM del ADMIN (canal de push exclusivo del administrador). -->
        <service
            android:name=".ui.service.FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FileProvider para entregar a la app de cámara una Uri temporal
             válida al hacer fotos de perfil con TakePicture. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>